Collect mod-contributed CODE tools (registered via
`AGENTS_SERVICE.registerOpTool`) into a toolset. Config `tools` names which
to include; empty = all. The third tool origin alongside workflow tools and
MCP, for tools a mod ships as plain functions.
