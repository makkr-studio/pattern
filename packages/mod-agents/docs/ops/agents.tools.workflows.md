Collects `boundary.tool` workflows into a toolset descriptor. Default: every
tool workflow in the app ("drop a file in workflows/, the agent learns it").
Name specific workflows in config to narrow an agent's toolbox.
