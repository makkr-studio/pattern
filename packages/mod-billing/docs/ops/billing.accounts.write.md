Create or update a billing account (upsert by name). Secrets/options accept
JSON objects — or JSON strings, which is what the admin form posts.
Idempotent: re-saving the same account converges. Privileged.
