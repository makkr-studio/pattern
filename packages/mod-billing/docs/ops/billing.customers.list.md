The user ↔ provider-customer mapping the webhooks maintain: status, prices,
and entitlement at a glance (admin → System → Billing → Customers). The first
place to look when "the subscription didn't stick" — if the row has no
userId, the checkout was started without one.
