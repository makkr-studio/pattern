Forget the caller's Buddy thread for a workflow slug — the dock's "New
conversation". Deletes the persisted messages (no-op without mod-store) and
answers `{ ok, cleared }`. Privileged: gate the trigger with the `admin`
scope.
