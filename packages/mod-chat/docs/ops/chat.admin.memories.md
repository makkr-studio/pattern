Every remembered fact, newest first (admin) — who it's about, the statement,
when it was learned, and the provenance ids (source run, conversation) behind
the Memories page's "Source run" link. Empty without mod-vectors or before
anything has been learned. Privileged: gate the trigger with the `admin` scope.
