Delete one remembered fact (admin): the Memories page's Forget button — the
right to be forgotten, one row at a time. The deletion is immediate; the next
turn simply doesn't recall it. Args { id }. Privileged: gate the trigger with
the `admin` scope.
