A conversation's turns folded into admin list rows: first user text, status,
event count, last error, duration, and the `runId` for a deep-link into the run
trace (the admin-as-agent-debugger story). `privileged`, admin-route-gated, no
scope check. For the full untruncated event log of one turn use
`chat.admin.turn`.
