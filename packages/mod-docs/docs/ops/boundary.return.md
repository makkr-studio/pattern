The matching out-gate for `boundary.manual`: whatever arrives on `value`
becomes the run's result (`result.outputs`). Use `boundary.return.named` when
a sub-workflow should return an object of named values instead of one.
