True if any element's **sub-workflow** result is truthy: the existential check. To test whether a literal value is in the list, `core.array.includes` is simpler; for the item itself use `core.array.find`. Runs one linked sub-run per element.
