The deep-equality test: use it to match an extracted field against an expected value and feed the boolean into a `core.flow.branch` or `core.flow.gate` condition. Comparison is structural; it equates objects or arrays with the same shape as well as scalars. For the inverse, reach for `core.cmp.neq`.
