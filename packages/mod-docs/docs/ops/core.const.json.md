The general-purpose literal: emit any JSON value (scalar, object, or array) from `config.value`, with an optional declared schema that validates it at author time. Reach for it when the shape is arbitrary or you want the editor to enforce a contract; for a plain object or array with no schema, the narrower `core.const.object`/`core.const.array` are clearer.
