Fires a named event with a payload: every `boundary.event` subscriber starts
its own run. Fire-and-forget: no result, no ordering. The decoupling tool for
"something happened, whoever cares can react".
