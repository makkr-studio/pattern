An AND-barrier as an explicit node: pulses `out` once ALL wired control-ins
have fired. Mostly unnecessary — any node with several wired control-ins
already waits for all of them — but useful to make a convergence point
visible and nameable on the canvas.
