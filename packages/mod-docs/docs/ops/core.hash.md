One-way digest of a string (`SHA-256` default; `hex` or `base64` out) for cache keys, dedup fingerprints, or content addressing. Keyless. For signing or verifying webhooks where a shared secret is involved, use `core.crypto.hmac`. Use a dedicated password KDF for passwords; this is a fast content-addressing digest.
