One-way digest of a string (`SHA-256` default; `hex` or `base64` out) for cache keys, dedup fingerprints, or content addressing. Keyless — for signing or verifying webhooks where a shared secret is involved, use `core.crypto.hmac` instead. Don't hash raw passwords with it; it's a fast digest, not a password KDF.
