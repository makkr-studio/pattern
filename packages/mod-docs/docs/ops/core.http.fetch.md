Outbound HTTP from a workflow — URL/method/headers/body in, status/headers/
body out (with a streaming body port for large responses). This is the
building block for API-calling tools: see `tool-weather.json` in the
agent-chat template for the canonical "tool = workflow that fetches" shape.
