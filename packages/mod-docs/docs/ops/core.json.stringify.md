Serialize a value to a JSON string; set `config.pretty` for 2-space indentation when the output is for humans. Reach for it when you need text (a log line, a request body, a file), not when wiring a value straight into a `boundary.http.response` body — the response gate serializes objects for you.
