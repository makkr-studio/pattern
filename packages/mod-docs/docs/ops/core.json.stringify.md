Serialize a value to a JSON string; set `config.pretty` for 2-space indentation when the output is for humans. Reach for it when you need text (a log line, a request body, a file). Wiring a value directly into a `boundary.http.response` body works without it; the response gate serializes objects automatically.
