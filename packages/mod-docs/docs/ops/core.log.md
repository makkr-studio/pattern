Structured logging from inside the graph: `value` passes THROUGH untouched,
so you can splice it into any value edge without rewiring. The line lands on
the node's span too: the admin's run view shows it in context.
