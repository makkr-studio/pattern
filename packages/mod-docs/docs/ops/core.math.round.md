Use to snap a number to the nearest integer before display or bucketing. Prefer `core.math.floor` or `core.math.ceil` when you need a directional bound rather than nearest, and `core.math.clamp` to bound a range.
