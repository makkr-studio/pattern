Deep copy via `structuredClone` — rarely needed, since the mutating object ops (`set`/`delete`/`merge`) already return fresh clones. Reach for it only to deliberately detach a value before handing it to something that mutates in place; note `structuredClone` rejects functions and other non-cloneable values.
