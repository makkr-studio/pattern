Choose one element at random from the wired `values` array, handy for sampling, A/B selection, or a random prompt variant from a `core.const.array`. An empty array yields `undefined`, so guard with `core.cast.isNull` downstream if the source can be empty. Non-deterministic: a replay won't reproduce the same choice.
