Emits a JSON Schema as a VALUE — wire it into ops that take schemas
(`core.schema.validate`, boundary config ports). Schemas are data here, like
everything else: build them, transform them, reuse them.
