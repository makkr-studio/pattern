Substring membership test, case-sensitive; normalize with `core.string.lower` first if needed. For position-anchored checks use `core.string.startsWith` / `core.string.endsWith`; for pattern matching use `core.string.match`.
