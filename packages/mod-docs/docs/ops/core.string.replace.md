Substitute matches of `config.search` with `config.replacement`. Literal by default; set `regex: true` to treat `search` as a pattern (then `$1` backrefs work in the replacement). `flags` defaults to `"g"`, so all occurrences go — drop the `g` for first-only. To extract matches rather than rewrite them, use `core.string.match`.
