Mustache-lite interpolation: `template` in config ("Hello, {{ name }}!"),
an object on the **`data`** input port, the rendered string on `out`. The
`data` port name trips people up — it's not `value`.
