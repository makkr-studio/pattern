Mustache-lite interpolation: `template` in config ("Hello, {{ name }}!"),
an object on the **`data`** input port, the rendered string on `out`. Note:
the input port is named **`data`**; wiring an object to `value` will not work.
