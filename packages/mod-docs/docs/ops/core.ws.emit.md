Send a message to one specific WebSocket connection — wire a single `message` value, or wire the `messages` stream port to push tokens chunk-by-chunk (e.g. relaying an agent's output to a socket). To reach everyone in a room use `core.ws.broadcast`; for the typed app-event envelope use `core.ws.notify`. The connection ref comes from a `boundary.ws.*` trigger.
