/** @pattern-js/mod-docs — well-known names. */

/** The built docs SPA (dist-app), served by `boundary.http.app`. */
export const DOCS_ASSETS_FS = "docs-assets";

/**
 * The docs host's OWN markdown chapter (the Pattern handbook), contributed
 * through the same public seam every other mod uses. Also homes the per-op
 * prose for core.* / boundary.* ops (no owning mod).
 */
export const DOCS_CONTENT_FS = "docs-content";
