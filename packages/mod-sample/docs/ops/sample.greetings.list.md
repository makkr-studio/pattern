The example of a declarative-page data source: a pure, no-input read with one named output. Front it with an `httpEndpoint` route (here `sample.route.greetings`) and point a Tier-1 `table` view (or a ⌘K command) at that route — never expose it raw. Demo only; copy the shape for your own read ops.
