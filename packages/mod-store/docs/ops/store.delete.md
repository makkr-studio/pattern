Delete a document by id; `ok` is true when a row went away, false when there was nothing to delete (idempotent — re-running is safe). There's no CAS here, so if a concurrent writer might re-create the doc, gate the delete behind a lease (`store.lease.acquire`) or check a status field first. Blobs are separate — use `store.blob.delete` for those.
