Extend a lease you already hold — a heartbeat for work that outlives the TTL. Use it inside long or streaming turns where the TTL would otherwise expire and let a second run steal the lock mid-flight (the chat pipeline renews `chat:conversation:{id}` as events stream). Renew with the *same* owner you acquired with via `store.lease.acquire`; like acquire it returns `ok:false` (rather than throwing) if the lease lapsed or someone else now holds it — branch on that, don't assume success.
