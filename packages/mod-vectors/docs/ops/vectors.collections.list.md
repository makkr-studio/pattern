List every vector collection with its embedding alias, locked dims, declared
filterables, row count and the active engine — the backing op for the admin's
Data → Vectors page. Privileged: gate the trigger with the `admin` scope.
